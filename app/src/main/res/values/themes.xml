<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash screen — Solum blue background + bell icon, transitions to main theme -->
    <style name="Theme.ESLCallApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/primary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/primary</item>
        <item name="postSplashScreenTheme">@style/Theme.ESLCallApp</item>
    </style>

    <style name="Theme.ESLCallApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:windowBackground">@color/background</item>
    </style>

    <!-- Full-screen alert: dark red, no action bar, no title -->
    <style name="Theme.ESLCallApp.Alert" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@color/alert_background</item>
        <item name="android:statusBarColor">@color/alert_background</item>
        <item name="android:navigationBarColor">@color/alert_background</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- History screen -->
    <style name="Theme.ESLCallApp.History" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:windowBackground">@color/background</item>
    </style>

</resources>
